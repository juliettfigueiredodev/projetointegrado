1- Métodos para melhorar tudo:
@email.setter 
    def email(self, new_email):

@password.setter
    def password(self, new_password):
       #verificar se a senha inserida tem caracteres permitidos. Se sim, retornar mensagem de cadastro realizado, se não raise ValueError com mensagem

def send_to_bd(self):
       receber os dados de user_id, user_type e dos setters para enviar para o BD

2- Tipos de acesso aos atributos, mantém ou muda?
    class User
    self.user_id = user_id
    self.user_type = user_type
    self.endereco = endereco
    self.telefone = telefone

    class Donor
    self.number_id = cpf
    self.name = name_donor
    self.date_birth = date_birth

    class Institution
    self.institution_reg_number = cnpj
    self.institution_name = institution_name




